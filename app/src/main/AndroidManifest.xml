<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.vesper.mobilecare">

    <!-- 1.0.0 第一位:代码重构,UI修改  第二位:更新功能  第三位:修复原有bug -->

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".SettingActivity">
        </activity>
        <activity android:name=".SetupOverActivity">
        </activity>
        <activity android:name=".userSetup.Setup1Activity">
        </activity>
        <activity android:name=".userSetup.Setup2Activity">
        </activity>
        <activity android:name=".userSetup.Setup3Activity">
        </activity>
        <activity android:name=".ContactListActivity">
        </activity>
        <activity android:name=".userSetup.Setup4Activity">
        </activity>
    </application>

</manifest>